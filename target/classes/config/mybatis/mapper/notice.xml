<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="notice">

	
	<select id="selectList" resultType="notice">
		select * from notice
	</select>
	
	<select id="selectOne" resultType="notice" parameterType="int">
		select * from notice where idx=#{idx}
	</select>
	
	<insert id="insert" parameterType="notice">
		insert into notice values(notice_seq.nextval,#{title},#{content},#{date})
	</insert>
	
	<update id="update" parameterType="notice">
		update notice set title=#{title},content=#{content},date=#{date} where idx=#{idx}
	</update>
	
	<delete id="delete" parameterType="int">
		delete notice where idx=#{idx}
	</delete>

</mapper>













